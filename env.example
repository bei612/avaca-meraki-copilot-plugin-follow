# Avaca Plugin Boilerplate - 环境变量配置示例

# ===========================================
# 🔧 核心框架配置
# ===========================================

# API 服务地址（生产环境必填）
NEXT_PUBLIC_API_BASE_URL=http://localhost:3402

# 🛡️ 权限配置（注意：不要使用引号包围值）

# 全局权限白名单（回退权限，当没有API级别权限时使用）
NEXT_PUBLIC_PLUGIN_WHITELIST=admin,super_user

# API级别权限配置（推荐使用，支持多种配置模式）
# 配置方式：
# 1. 用户列表：NEXT_PUBLIC_API_PERMISSION_[API名称]=user1,user2,user3
# 2. 允许所有：NEXT_PUBLIC_API_PERMISSION_[API名称]= (空字符串)
# 3. 拒绝所有：NEXT_PUBLIC_API_PERMISSION_[API名称]=none 或 deny
# 4. 使用全局：不设置该环境变量

# Meraki 网络管理权限（特定用户列表）
NEXT_PUBLIC_API_PERMISSION_MERAKI_NETWORKS=admin,network_admin,80a78507-22e3-4284-a852-9255057af933

# 用户管理权限示例（不同配置模式）
# NEXT_PUBLIC_API_PERMISSION_USER_MANAGEMENT=admin,hr_manager  # 特定用户
# NEXT_PUBLIC_API_PERMISSION_PUBLIC_API=                      # 空字符串=允许所有用户
# NEXT_PUBLIC_API_PERMISSION_SENSITIVE_API=none               # 拒绝所有用户
# NEXT_PUBLIC_API_PERMISSION_REPORTS_DATA=                    # 不设置=使用全局权限

# 其他框架配置
NEXT_PUBLIC_ENABLE_DYNAMIC_PERMISSIONS=true
NEXT_PUBLIC_DEFAULT_ACCESS=false

# API 请求超时时间（毫秒）
NEXT_PUBLIC_API_TIMEOUT=10000

# ===========================================
# 📱 业务层配置 - Meraki示例
# ===========================================

# Meraki API 配置（真实API调用）
# 获取方式：登录 https://dashboard.meraki.com → My Profile → API Access
MERAKI_API_KEY=your_meraki_api_key_here
MERAKI_BASE_URL=https://api.meraki.cn/api/v1
MERAKI_ORGANIZATION_ID=your_organization_id_here

# 注意：
# 1. 如果不配置这些环境变量，插件会自动使用模拟数据
# 2. 真实API调用失败时会自动回退到模拟数据
# 3. 支持中国区域的 Meraki API (api.meraki.cn)

# ===========================================
# 🔗 其他业务配置示例
# ===========================================

# 添加您的业务API配置
# YOUR_BUSINESS_API_KEY=your_api_key
# YOUR_BUSINESS_BASE_URL=https://api.example.com
# YOUR_BUSINESS_SETTINGS=configuration

# ===========================================
# 🛠️ 开发配置
# ===========================================

NODE_ENV=development
